apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallet-postgres-pvc
  namespace: freelance
  labels:
    app: wallet-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
